#ifndef TYPE_HPP
#define TYPE_HPP

#include <vector>

using ID = int;
using input_type = std::vector<int>;
using key_input_type = std::pair<ID, input_type>;
using register_input_type = std::pair<ID, input_type>;
using input_return_type = std::pair<ID, input_type>;

using IP = std::string;

#endif // TYPE_HPP
